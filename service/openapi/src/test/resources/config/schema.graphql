schema {
  query: Query
}

type Query {
  breweryCollection: [Brewery!]!
}

type Brewery {
  identifier: ID!
  name: String!
  postalAddress: Address
}

type Address {
  street: String!
  postalCode: String
}
