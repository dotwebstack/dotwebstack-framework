schema {
  query: Query
}

type Query {
  breweryCollection(filter: NameFilter): [Brewery!]!
  breweryPageableCollection(first: Int = 10, offset: Int = 0): BreweryConnection!
  brewery(identifier: ID!): Brewery
}

input NameFilter {
  name: StringFilter
}

input StringFilter {
 in: [String]!
}

type Brewery {
  identifier: ID!
  name: String!
  postalAddress: Address
  numberOfEmployees: Int
}

type BreweryConnection {
  nodes: [Brewery!]!
}

type Address {
  street: String!
  postalCode: String
}
