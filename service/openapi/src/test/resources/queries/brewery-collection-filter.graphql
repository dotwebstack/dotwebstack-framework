query Query {
  breweryCollection(filter: {name : {like : "id1%"}, identifier : {in : ["breweryname"]}}) {
    identifier
    name
    postalAddress {
      street
      postalCode
    }
    numberOfEmployees
  }
}