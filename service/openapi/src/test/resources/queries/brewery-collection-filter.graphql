query Query {
  breweryCollection(filter: {name : {like : "id1%"}, identifier : {in : ["breweryname"]}, employees : {not : {gt : 10}}}) {
    identifier
    name
    postalAddress {
      street
      postalCode
    }
    numberOfEmployees
  }
}