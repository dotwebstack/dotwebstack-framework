schema {
  query: Query
}

type Query {
  beers: [Beer!]!
  beer(identifier: ID!): Beer!
  breweries: [Brewery!]!
  brewery(identifier: ID!): Brewery!
}

type Brewery {
  identifier: ID!
  name: String!
  status: String!
  beers: [Beer]
}

type Address {
  identifier: String!
  street: String!
  city: String!
}

type Beer {
  identifier: ID!
  name: String!
  abv: String
  brewery: Brewery!
  ingredients: [Ingredient!]!
}

type Ingredient {
  identifier: ID!
  name: String!
}