spring:
  main:
    banner-mode: "off"
  jersey:
    type: filter

logging:
  level:
    ROOT: ERROR

ldt:
  config:
    location:
      openapi: file:src/test/resources/openapi/*
      model: file:src/test/resources/model/**
