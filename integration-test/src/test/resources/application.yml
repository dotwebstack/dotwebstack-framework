spring:
  main:
    banner-mode: "off"
  jersey:
    type: filter

logging:
  level:
    ROOT: OFF

dotwebstack:
  config:
    resourcePath: file:src/test/resources
    host: localhost
    systemGraph: http://dbeerpedia.org#Theatre
