version: '3.9'

services:
  postgres:
    image: postgres:11
    ports:
      - 5432:5432
    volumes:
      - ./src/main/resources/initdb:/docker-entrypoint-initdb.d
    environment:
      POSTGRES_PASSWORD: postgres
