logging:
  level:
    org.dotwebstack: DEBUG

dotwebstack:
  config: dotwebstack-context.yaml
  postgres:
    host: localhost
    port: 5432
    username: postgres
    password: postgres
    database: postgres
    schema: dbeerpedia
