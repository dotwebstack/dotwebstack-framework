openapi: 3.0.2

info:
  title: Ruimtelijke Plannen API
  version: 3.0.0

components:
  schemas:
    Plan:
      type: object
      required:
        - id
        - naam
      properties:
        id:
          type: string
        naam:
          type: string
          nullable: true

paths:
  /plan:
    get:
      x-dws-query: plan
      responses:
        200:
          description: OK
          content:
            application/hal+json:
              schema:
                $ref: '#/components/schemas/Plan'